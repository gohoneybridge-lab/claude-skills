// web.core_web_vitals — server component, no client JS by default, no unsized
// media (add next/image with explicit width/height when you add a hero). This
// is the fill point: replace with the product's real first screen.
export default function Home() {
  return (
    <section>
      <h1>Golden Web Seed</h1>
      <p>
        Replace this with the product. The plumbing (tests, CI gate, security
        headers, observability, server-side auth) is already wired — see README.
      </p>
    </section>
  );
}
